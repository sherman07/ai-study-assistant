/**
 * Public shared/ui surface.
 * Truly cross-cutting presentational primitives only.
 * Feature-specific UI belongs under features/*/ui.
 *
 * No shared UI primitives extracted yet — import from feature or page modules.
 */
export {};
